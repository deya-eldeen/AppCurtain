# Plan

- Initialize a Swift package named AppCurtain with iOS support and a library target.
- Implement a small, focused API to apply a privacy curtain (blur or mask) when the app resigns active and remove it when foregrounded.
- Add basic documentation and usage notes.

